# CNN-Emotional-Recognition
Creates various models to recognize emotions by training custom models, or using pretrained ones.

The datasets referenced in each model will provide a split in of training and test models. You need to combine both these folders into 1. The code already takes care of creating the train, test, and validation sets.
